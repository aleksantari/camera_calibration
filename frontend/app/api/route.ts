import { type NextRequest, NextResponse } from "next/server"

// This is a placeholder API route that will be rewritten to your Flask backend
// The actual implementation will be handled by your Flask backend
export async function GET(request: NextRequest) {
  return NextResponse.json({ message: "This route is rewritten to Flask backend" })
}

